spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: mbo
test:
  topic: embedded-test-topic
  groupid: mbo

monitor:
  kafka:
    bootstrap:
      config: "PLAINTEXT://localhost:9085"
    consumer:
      groupid: "mboGrp"